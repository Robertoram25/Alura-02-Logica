/*let altura = prompt('Ingresa tu estatura en metros: ');
let peso = prompt('Ingresa tu peso en kilogramos: ');

function calcularIMC(altura, peso) {
    let res = parseInt(peso/(altura*altura));
    console.log(res);
}

calcularIMC(altura, peso);
*/

/*let dolares = prompt('Ingresa una cantidad en dolares: ');

function conversion(dolares) {
    let reales = parseInt(dolares * 4.8);
    alert(`${dolares} dólares equivales a ${reales} reales`);
}

conversion(dolares);*/

